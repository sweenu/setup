# Start the VPN by starting openvpn-client@TunnelBear<region>.service
---
- name: Copy config files, certificates and login.key
  copy:
    src: ./
    dest: /etc/openvpn/client/
    mode: 0600
  become_user: root
