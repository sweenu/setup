GRUB_TIMEOUT=0
GRUB_DISTRIBUTOR=Arch
GRUB_CMDLINE_LINUX_DEFAULT='quiet splash'
{% if use_encryption %}
GRUB_CMDLINE_LINUX='rd.luks.name={{ lookup('pipe', 'blkid -s UUID -o value ' + disk + root_partition) }}={{ dm_name }}'
{% endif %}
