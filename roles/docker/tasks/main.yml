---
- name: Install Docker
  pacman:
    name: docker
  become: true

- name: Add user '{{ username }}' to docker group
  user:
    name: '{{ username }}'
    groups: docker
    append: yes
  become: true

- name: Install docker-compose
  pacman:
    name: docker-compose
  become: true
