#!/usr/bin/perl

scalar {
  ALSI_VERSION         => "0.4.7",
  COLORS_FILE          => "{{ config_home }}/alsi/alsi.colors",
  DE_FILE              => "{{ config_home }}/alsi/alsi.de",
  DEFAULT_COLOR_BOLD   => "blue",
  DEFAULT_COLOR_NORMAL => "blue",
  DF_COMMAND           => "df -Th -x sys -x tmpfs -x devtmpfs &>/dev/stdout",
  GTK2_RC_FILE         => "",
  GTK3_RC_FILE         => "{{ config_home }}/gtk-3.0/settings.ini",
  LOGO_FILE            => "{{ config_home }}/alsi/alsi.logo",
  OUTPUT_FILE          => "{{ config_home }}/alsi/alsi.output",
  PACKAGES_PATH        => "/var/lib/pacman/local/",
  PS_COMMAND           => "ps -A",
  SCREENSHOT_COMMAND   => "maim -d 5 > /home/sweenu/Desktop/alsi_`date +%s`.png",
  USAGE_COLORS         => 1,
  USAGE_COLORS_BOLD    => 0,
  USAGE_PRECENT_GREEN  => 50,
  USAGE_PRECENT_RED    => 100,
  USAGE_PRECENT_YELLOW => 85,
  USE_LOGO_FROM_FILE   => 0,
  USE_VALUES_COLOR     => 0,
  WM_FILE              => "{{ config_home }}/alsi/alsi.wm",
}
